

#crypto_currency = 'ETH'
#against_currecny = 'USD'

#start = dt.datetime(2019,1,1)
#end = dt.datetime.now()

#data = web.DataReader(f'{crypto_currency}-{against_currecny}','yahoo',start,end)

#prepare the data
#print(data.head())
#Scale down the data to range [0,1]